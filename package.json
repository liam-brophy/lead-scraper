{
  "name": "lead-scraper",
  "version": "1.0.0",
  "description": "Scored, filterable outreach lead pipeline for freelance web design prospecting. Discovery and scoring only -- no automated outreach.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js",
    "cli": "node cli.js"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "cookie": "^0.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "p-limit": "^3.1.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.14"
  }
}
